# Verification: cards

How to run this file: bring the surface up the way [the protocol](README.md)
says — `bun run dev` on a phone on the same network, or the deployed app, which
is the better choice for anything involving a second device — and confirm
`git rev-parse --short HEAD` reads `b46f330` first. A fresh state for this
cluster means clearing site data for the origin, which removes the `wwbh:` keys
these documents depend on (`wwbh:vault-favourites` and its shelf order twin
`wwbh:vault-layout`, `wwbh:trophy-seen`, `wwbh:sfx-muted`, `wwbh:guest-token`,
`wwbh:member-token`, `wwbh:member-name`, `wwbh:was-member`, `wwbh:guest-name`,
`wwbh:device-id`, `wwbh:trade-seen` and the `wwbh:v…` art-URL snapshots) and the
IndexedDB database `wwbh-cards`, which holds the collection and the day's pack
under the `pack-state` store at key `today`; `wwbh-combine` belongs to the timing
console and is irrelevant here. **Clear deliberately, not between items**: a new
guest gets a fresh daily secret and loses everything the old one held, and a
cleared `wwbh:trophy-seen` makes the next launch swallow every trophy ceremony
silently. Most items here need a collection, so budget league days: one pack a
day, one secret a day, and a streak that only builds on consecutive days. Start
each pass with reduced motion off, card sound unmuted, Tilt off, the browser's
motion permission not yet granted, the device clock automatic, the vault in its
default layout with every shelf open and the Name sort selected, and dust off
unless the row says otherwise. Device values in this file: **phone** is the real
surface and the default; **desktop** is a wide browser, used where a row needs
devtools, a keyboard, or the wide layout that puts six chips on one line;
**guest** is a browser with site data cleared and no player claimed; **member**
holds a member token claimed from a paper code; **account** is signed in with
email and password, which is what a milestone claim requires; **commissioner**
is an admin session, needed to grant, retire, rename or hide a card or a set;
**second device** is genuinely a second handset — a second tab shares storage
and IndexedDB, and several items turn on that difference, so where a row says
second tab it means a tab and where it says second device it means a phone;
**second person** is somebody else's member session, for trades; **offline** is
airplane mode on the handset rather than a devtools toggle; **reduced motion**
is the OS-level setting, not a control in the app; **gyroscope** is a real phone
with motion permission, and the permission prompt is itself an item; **race
day** marks items that can only honestly be checked while a combine is running.

## cards/favourites.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| VLT-01 | P1 | phone, guest | Tapping the star fills it and raises a shelf called "Favourites" at the top of the vault with that card on it ([The simple case](../cards/favourites.md#the-simple-case)). | A vault holding at least two roster cards, nothing pinned. | 1. Scroll to a face-up roster tile.<br>2. Tap the star in its top-right corner.<br>3. Look at the top of the page. | The star fills, a Favourites shelf appears at the top of the page holding that card, and nothing was submitted or confirmed. | pass (scripted) — shelf raised on first pin |
| VLT-02 | P1 | desktop, guest | Pinning writes the id to `wwbh:vault-favourites` ([The tap that starts something](../cards/favourites.md#the-tap-that-starts-something)). | A desktop browser on the vault with devtools open, nothing pinned. | 1. Confirm `wwbh:vault-favourites` is absent.<br>2. Pin one roster card.<br>3. Read the key. | The key holds `{"ids":["p:<uuid>"]}` — one id, prefixed, and nothing else. No request was made. | pass (scripted) — key written in that shape |
| VLT-03 | P1 | phone, guest | The star's label names the card and the direction of travel, and reports its state through `aria-pressed` ([Accessibility](../cards/favourites.md#interactions-with-other-systems)). | A vault with an unpinned card whose player's name is known, a screen reader running. | 1. Move the screen reader onto the star of an unpinned card.<br>2. Write down what it announces. | It reads "Pin <player name> to the top" and reports not-pressed. The star glyph itself is not announced, and the control is a toggle rather than a link. | pass (scripted) — label and aria-pressed=false confirmed |
| VLT-04 | P1 | phone, guest | Pinning flips the same control's label to the other direction ([Accessibility](../cards/favourites.md#interactions-with-other-systems)). | Immediately after VLT-03. | 1. Activate the star.<br>2. Read the label again without moving focus. | It now reads "Unpin <player name> from the top" and reports pressed. The announcement lands on the button the user just operated rather than on a different control. | pass (scripted) — label flips in place |
| VLT-05 | P1 | phone, guest | There is no empty Favourites shelf — it is absent, not empty ([Edge cases](../cards/favourites.md#edge-cases)). | A vault with nothing pinned, `wwbh:vault-favourites` absent. | 1. Open the vault and read every shelf heading top to bottom. | No shelf named Favourites anywhere on the page, and no placeholder saying nothing is pinned yet. | pass (scripted) — no shelf with nothing pinned |
| VLT-06 | P1 | phone, guest | Unpinning the last pinned card removes the shelf entirely rather than leaving it empty ([The simple case](../cards/favourites.md#the-simple-case)). | Exactly one card pinned. | 1. Tap the filled star on the pinned card, on the Favourites shelf.<br>2. Look at the top of the page. | The shelf is gone, not empty. The card is back on the shelf it came from. | pass (scripted) — shelf removed with the last pin |
| VLT-07 | P2 | desktop, guest | A second pin is appended to the end of the list rather than the front ([The tap that starts something](../cards/favourites.md#the-tap-that-starts-something)). | One card already pinned, devtools open on the key. | 1. Note the single id in `wwbh:vault-favourites`.<br>2. Pin a second card.<br>3. Read the key and the shelf. | The new id is second in the list, and the new card is to the right of the first on the shelf. The card pinned first has not been shoved down the page. | pass (scripted) — appended, not prepended |
| VLT-08 | P1 | desktop, guest | Anything under the key that is not a list of strings reads as an empty shelf rather than an error ([Edge cases](../cards/favourites.md#edge-cases)). | A desktop browser with one card pinned. | 1. Replace the value of `wwbh:vault-favourites` with `{"nope":1}`.<br>2. Reload the vault.<br>3. Replace it with `not json at all` and reload again. | Both times the vault renders with no Favourites shelf and no error. Every other shelf is intact, and pinning a card afterwards works normally. | pass (scripted) — junk reads as empty |
| VLT-09 | P1 | phone, guest | A pinned card moves to Favourites rather than appearing twice ([The simple case](../cards/favourites.md#the-simple-case)). | A vault with a roster shelf and at least one secret shelf. | 1. Note which shelf a card sits on and that shelf's count.<br>2. Pin the card.<br>3. Read both shelves again. | The card is on Favourites and gone from its old shelf, whose count has dropped by one. It is not on the page twice. | — |
| VLT-10 | P2 | desktop, member | Only the id is stored — never the edition the card is wearing ([The tap that starts something](../cards/favourites.md#the-tap-that-starts-something)). | A held card wearing a special finish, devtools open. | 1. Pin the card.<br>2. Read `wwbh:vault-favourites` in full. | The value carries an id and nothing else — no finish, no tier, no count, no name. | — |
| VLT-11 | P2 | desktop, member | Ids carry a prefix — `p:` for a roster card, `s:` for a secret — so the two id spaces can never collide ([Arrive](../cards/favourites.md#arrive)). | A vault holding both a roster card and a secret. | 1. Pin one roster card and one secret.<br>2. Read the key. | Two ids, one beginning `p:` and one beginning `s:`. Both cards are on the Favourites shelf. | — |
| VLT-12 | P1 | phone, guest | The tap on the star does not reach the card underneath ([The tap that starts something](../cards/favourites.md#the-tap-that-starts-something)). | The vault, on a roster tile whose body links to the player page. | 1. Tap the star, dead centre, ten times in a row — pinning and unpinning five times.<br>2. Watch the address bar. | The star toggles every time and the player page never opens. The vault stays where it was, at the same scroll position. | — |
| VLT-13 | P2 | phone, guest | No star is drawn on a locked card, on the tile or on the player page ([Summary](../cards/favourites.md#summary)). | A vault with at least one face-down roster slot. | 1. Look at the top-right corner of a face-down tile.<br>2. Open that player's page and look through the settings for a Pin chip. | No star on the tile and no Pin chip on the page. There is no copy to pin and nothing that could go on the shelf. | — |
| VLT-14 | P2 | phone, guest | The pinned list is read after the first paint, never during it, so the vault renders as though nothing is pinned and the shelf appears a moment later ([Arrive](../cards/favourites.md#arrive)). | Two or three cards pinned; a screen recording running. | 1. Record the screen.<br>2. Reload the vault.<br>3. Step through the first second of the recording frame by frame. | The first frame has no Favourites shelf; it appears a frame or two later. No error and no layout jump beyond the shelf arriving. | — |
| VLT-15 | P2 | phone, guest | The vault's grid and the player page share one shelf, so pinning from one lights the control on the other immediately ([While it runs](../cards/favourites.md#while-it-runs)). | A held card, unpinned, and its player page reachable. | 1. Pin the card from the vault.<br>2. Open that player's page without reloading.<br>3. Unpin from the page and go back to the vault. | The page's chip already reads Pinned when it opens, and the vault's star is empty again after the unpin, with no reload in either direction. | — |
| VLT-16 | P2 | phone, member | The same toggle appears on a player card as a chip reading "Pin" or "Pinned", among that page's settings alongside Tilt ([Summary](../cards/favourites.md#summary)). | A held card's player page, on a wide enough window that the chips are visible. | 1. Find the chip and read its word.<br>2. Note which row it sits in.<br>3. Tap it and read it again. | It reads Pin, sits with Tilt and Sound rather than with Flip, Share and Compare, and reads Pinned after the tap. | — |
| VLT-17 | P2 | phone, guest | A pin survives a reload and a navigation ([Cancel and interrupt](../cards/favourites.md#cancel-and-interrupt)). | One card pinned. | 1. Reload the vault and check the shelf.<br>2. Navigate to the board and back and check again.<br>3. Close the tab, reopen the app and check again. | The Favourites shelf and the filled star are there all three times, on this browser. | — |
| VLT-18 | P2 | offline, phone, guest | Pinning is fully functional with the radio off; there is no request to fail ([Offline and reconnection](../cards/favourites.md#interactions-with-other-systems)). | The vault loaded, then airplane mode. | 1. Airplane mode.<br>2. Pin two cards and unpin one.<br>3. Reload while still offline. | Every toggle lands instantly with no spinner and no failure. The surviving pin is still there after the offline reload. | — |
| VLT-19 | P2 | phone, member | Claiming a player or signing out neither carries pins across nor clears them; they belong to the browser ([Modifiers](../cards/favourites.md#modifiers)). | A guest with two cards pinned, and a paper code. | 1. Note the two pinned cards.<br>2. Claim a player.<br>3. Read the shelf.<br>4. Sign out and read it again. | The same two cards are pinned in the same order after the claim and after the sign-out. Nothing was added and nothing was lost. | — |
| VLT-20 | P2 | second device, member, account | Nothing about favourites is synchronised: two phones on one account hold the same collection and two different shelves ([The second device](../cards/favourites.md#interactions-with-other-systems)). | Phone A: signed in, two cards pinned. Phone B: signed in to the same account, nothing pinned. | 1. Open the vault on phone B.<br>2. Look for a Favourites shelf.<br>3. Pin a different card on B and re-check A. | Phone B has no Favourites shelf until it pins something of its own, and the pin made on B never appears on A. | — |
| VLT-21 | P2 | desktop, guest | A pin made in another tab arrives and redraws this one ([Cancel and interrupt](../cards/favourites.md#cancel-and-interrupt)). | Two tabs of the same browser, both on the vault, nothing pinned. | 1. In tab 1, pin a card.<br>2. Switch to tab 2 without reloading it.<br>3. Unpin from tab 2 and switch back to tab 1. | Each tab picks up the other's change without a reload, and the two never disagree about what is on the shelf. | — |
| VLT-22 | P2 | phone, guest | Where the browser refuses to store, the star still fills and the shelf still appears; the pin is lost on the next reload ([It settles](../cards/favourites.md#it-settles)). | A private-mode window, or storage blocked for the origin, on the vault. | 1. Pin a card and watch the star.<br>2. Confirm the Favourites shelf appears.<br>3. Reload. | The star fills and does not un-fill under the thumb, and the shelf appears. After the reload the pin is gone, with no error at any point. | — |
| VLT-23 | P2 | phone, member | Pinning every card of a secret set makes that set's shelf disappear from the lower half of the page ([Edge cases](../cards/favourites.md#edge-cases)). | A secret set the collection holds exactly two or three cards from, all unpinned. | 1. Note the set's shelf and its count.<br>2. Pin every card on it.<br>3. Read the page.<br>4. Unpin one. | The set's shelf is gone once its last card moves up, and it returns with one card when one is unpinned. | — |
| VLT-24 | P2 | phone, member, second person | A card traded away stops appearing on the shelf while the id stays stored, so the pin returns if the card ever comes back ([Cancel and interrupt](../cards/favourites.md#cancel-and-interrupt)). | A pinned spare, and a second person willing to trade it away and back. | 1. Pin a card and note its position in the key.<br>2. Trade the card away and reopen the vault.<br>3. Read the key.<br>4. Trade it back and reopen the vault. | The card leaves the shelf silently while its id stays in the key, and when it comes back it reappears in its original position in the pin order. | — |
| VLT-25 | P3 | phone, guest | The star fills through a colour transition and nothing else: no chime, no animation ([Motion and sound](../cards/favourites.md#interactions-with-other-systems)). | Sound unmuted, reduced motion off, on the vault. | 1. Turn the volume up.<br>2. Pin and unpin a card five times, listening and watching. | Silence both ways, and the only movement is the star's colour. Nothing bounces, scales or sparkles. | — |

Not checkable by hand:

- Which position the Favourites shelf takes when a previously emptied shelf comes back, on a device that has reordered its vault ([Open questions and verification](../cards/favourites.md#open-questions-and-verification)). It is a layout-merge question, and the answer depends on stored order the tester cannot see the shape of.
- Whether a card pinned against a previous combine reappears if that combine becomes active again ([Open questions and verification](../cards/favourites.md#open-questions-and-verification)) — it needs two combines and a device that pinned against the older one.
- That no other feature reads or writes the favourites key ([Open questions and verification](../cards/favourites.md#open-questions-and-verification)). A stray write would present as junk, which VLT-08 already covers; the absence of other writers is a source claim.
- Whether the star being dimmed rather than hidden-until-hover is the right call ([Modifiers](../cards/favourites.md#modifiers)) — a design decision, not a behaviour.

## cards/the-vault.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| VLT-26 | P1 | phone, member | The vault is the screen the app opens to ([Summary](../cards/the-vault.md#summary)). | Any identity. | 1. Open the app at `/`.<br>2. Read the screen and the lit tab. | The vault is drawn and Vault is lit. | — |
| VLT-27 | P2 | phone, member | The header counts what is printed and what you have collected, and puts the Open Pack button beside it ([The simple case](../cards/the-vault.md#the-simple-case)). | A member holding some of the roster, today's secret unspent. | 1. Read the header line under the title.<br>2. Find the Open Pack button. | The line reads "<n> of <n> cards printed · <n> collected" and the Open Pack button is in the header, within thumb reach rather than at the foot of the page. | — |
| VLT-28 | P1 | phone, member | The collected count waits for the server rather than trusting the device, so the header fills in a beat after the page draws ([Arrive](../cards/the-vault.md#arrive)). | A member with a collection, a screen recording running. | 1. Record the screen and reload the vault.<br>2. Step through the first second. | The count is absent on the first frames and appears once, at its real value. It never starts at a different number and corrects itself. | — |
| VLT-29 | P1 | phone, member | Until the collection reconciles the page treats every card as locked, because a face-down card going face-up is a reveal and the reverse is a leak ([Arrive](../cards/the-vault.md#arrive)). | A member with several held cards, on a throttled connection. | 1. Reload the vault and watch the roster shelf from the first frame.<br>2. Note the direction every tile moves in. | Tiles start face-down and turn face-up as the collection lands. No tile is ever face-up first and then face-down. | — |
| VLT-30 | P2 | phone, member | The Open Pack button wears a coloured ring and dot while today's secret is still waiting ([Notifications and badges](../cards/the-vault.md#interactions-with-other-systems)). | Today's secret unspent. | 1. Read the Open Pack button.<br>2. Tear the pack and pull the secret.<br>3. Return to the vault. | The ring and dot are there beforehand and gone afterwards, in the secret's own colour rather than a generic accent. | — |
| VLT-31 | P1 | phone, member | The Rarity sort orders held cards best-first while locked slots stay in name order and say nothing about each other ([Edge cases](../cards/the-vault.md#edge-cases)). | A vault with four or five held cards across different tiers and a dozen locked slots. | 1. Tap Rarity.<br>2. Read the held cards in order, then the locked ones. | Held cards are ordered by tier with the better ones first; every locked slot sits after them, in name order. A platinum copy of a DNF card never outranks a base champion. | — |
| VLT-32 | P2 | phone, member | Shuffle stays lit until one of the four sorts is tapped, and tapping it again reshuffles ([Edge cases](../cards/the-vault.md#edge-cases)). | The vault in Name order. | 1. Tap Shuffle and note the order.<br>2. Tap Shuffle again and note the new order.<br>3. Tap Rarity. | The grid reorders both times and Shuffle stays lit through the second tap. Tapping Rarity unlights Shuffle and sorts. | — |
| VLT-33 | P2 | race day, phone, member | Shuffle is seeded on the event, so a result landing does not silently reshuffle the grid under your thumb ([Modifiers](../cards/the-vault.md#modifiers)). | A shuffled vault during a live combine, and a commissioner able to save a result. | 1. Shuffle and photograph the grid.<br>2. Have the commissioner save a result.<br>3. Compare the grid. | Tiers redraw where they changed, and the card order is unchanged. | — |
| VLT-34 | P2 | phone, member | The sort and the shuffle are per-visit: the page comes back in Name order ([Leave without acting](../cards/the-vault.md#leave-without-acting)). | A vault sorted by Rarity or shuffled. | 1. Sort by Rarity.<br>2. Navigate to the Board and back.<br>3. Read which sort is lit. | Name is lit and the grid is in name order. Nothing about the sort was remembered. | — |
| VLT-35 | P2 | desktop, member | Rolling a shelf up stores the shelf, not the arrangement ([The tap that starts something](../cards/the-vault.md#the-tap-that-starts-something)). | A vault with more than one shelf, `wwbh:vault-layout` absent, devtools open. | 1. Confirm the key is absent.<br>2. Roll one shelf up by tapping its header.<br>3. Read `wwbh:vault-layout`. | The stored value names the closed shelf. It does not contain a full ordering of the page. | — |
| VLT-36 | P2 | desktop, member | The first move pins everything currently on screen, not just the shelf that moved ([The tap that starts something](../cards/the-vault.md#the-tap-that-starts-something)). | A vault with three or more shelves and no stored order. | 1. Tap Rearrange.<br>2. Move one shelf up one place.<br>3. Tap Done and read `wwbh:vault-layout`. | The stored order lists every shelf on the page, not a single entry for the one that moved. | — |
| VLT-37 | P2 | phone, member | Closed shelves are what is stored, never open ones, so a set appearing for the first time arrives open ([The tap that starts something](../cards/the-vault.md#the-tap-that-starts-something)). | A vault with one shelf rolled up, and a pull from a set the device has never held. | 1. Roll one shelf up.<br>2. Pull a secret from a set with nothing on the page yet.<br>3. Return to the vault. | The new set's shelf is open with its card visible. The shelf rolled up in step 1 is still rolled up. | — |
| VLT-38 | P2 | phone, member | A rolled-up shelf drops its cards from the page entirely rather than hiding them ([Edge cases](../cards/the-vault.md#edge-cases)). | A vault with a shelf holding several tiles, on a phone that heats up under load. | 1. Roll the roster shelf up.<br>2. Scroll the page and note how it responds.<br>3. Roll it back down. | The shelf collapses to a title bar and the page scrolls noticeably more smoothly; the tiles are gone rather than merely clipped. | — |
| VLT-39 | P2 | phone, member | The Rearrange toggle only appears when there is more than one shelf to move ([Edge cases](../cards/the-vault.md#edge-cases)). | A brand-new guest holding nothing but the roster shelf. | 1. Open the vault and look for Rearrange.<br>2. Pull a secret so a second shelf exists and look again. | Absent with one shelf, present with two. | — |
| VLT-40 | P2 | phone, member | A move at either end of the list does nothing rather than wrapping ([Edge cases](../cards/the-vault.md#edge-cases)). | A vault with three shelves, in Rearrange mode. | 1. Press the up arrow on the topmost shelf.<br>2. Press the down arrow on the bottom shelf. | Neither shelf moves and neither jumps to the other end of the page. The arrow at the end is marked unavailable rather than removed, so focus is not lost. | — |
| VLT-41 | P1 | phone, member | The arrangement survives a reload and belongs to the browser ([It settles](../cards/the-vault.md#it-settles)). | A vault whose shelves have been reordered and one rolled up. | 1. Note the order and which shelf is closed.<br>2. Reload.<br>3. Close the app, reopen and check again. | The same order and the same closed shelf both times. | — |
| VLT-42 | P2 | second device, member, account | A second phone on the same account gets the collection and its own default layout ([The second device](../cards/the-vault.md#interactions-with-other-systems)). | Phone A: signed in, shelves rearranged. Phone B: signed in to the same account, never arranged. | 1. Open the vault on phone B.<br>2. Compare the shelf order and the collection with phone A. | The same cards, in the default shelf order, every shelf open. The arrangement did not travel. | — |
| VLT-43 | P2 | phone, member | Where the browser refuses to store, shelves still move for this page load and the arrangement is gone on the next reload ([It settles](../cards/the-vault.md#it-settles)). | A private-mode window with more than one shelf on the page. | 1. Rearrange two shelves and roll one up.<br>2. Confirm the page obeys.<br>3. Reload. | The shelves move under the thumb without springing back, and the page returns to defaults after the reload, with no error. | — |
| VLT-44 | P2 | phone, member | A shelf only exists once there is something on it: Favourites, Complete and each secret set are absent until earned, and Roster is last by default ([What is on a shelf](../cards/the-vault.md#what-is-on-a-shelf)). | A brand-new member holding nothing but roster cards from one pack. | 1. Read every shelf heading top to bottom.<br>2. Pin a card and read again.<br>3. Finish a set — or take one already finished — and read again. | With nothing earned there is only Roster. Favourites appears on the first pin, Complete on the first finished set, and Roster stays at the bottom throughout. | — |
| VLT-45 | P2 | phone, member | A roster whose every card has been pinned upstairs says "Every card is pinned to Favourites", which is a different state from "No participants yet." ([Edge cases](../cards/the-vault.md#edge-cases)). | A vault where every held roster card can be pinned, on a small roster. | 1. Pin every card on the roster shelf.<br>2. Read the roster shelf's body. | It says every card is pinned to Favourites. It does not say there are no participants and it does not go blank. | — |
| VLT-46 | P2 | phone, guest | A face-down slot reads "Not packed yet", announces itself as "<player> — not packed yet", and still links to that player's page ([Edge cases](../cards/the-vault.md#edge-cases)). | A vault with face-down slots, a screen reader running. | 1. Read the caption under a face-down slot.<br>2. Move the screen reader onto it.<br>3. Tap it. | The caption and the announcement both name the player and say it is not packed. The tap opens that player's gated page rather than doing nothing. | — |
| VLT-47 | P2 | phone, member | The secrets line, the packs line and the streak line are absent at zero rather than showing a zero ([Edge cases](../cards/the-vault.md#edge-cases)). | A brand-new guest that has never torn a pack. | 1. Open the vault and read the header block line by line. | No "0 secrets pulled", no "0 packs", no flame with a zero. The lines are simply not there. | — |
| VLT-48 | P2 | phone, member | With dust on, a member gets a balance chip beside the title that links to the shop; with dust off there is no chip and no balance is asked for ([Modifiers](../cards/the-vault.md#modifiers)). | A member with a dust balance, and a commissioner able to flip the switch. | 1. With dust on, read the header and tap the chip.<br>2. Have the commissioner switch dust off.<br>3. Return to the vault. | The chip is beside the title and opens the shop. With dust off the chip is gone and nothing in its place says a balance failed to load. | — |
| VLT-49 | P2 | offline, phone, member | Card art that has not been fetched falls back to the player's initials, and counters that never answered stay blank rather than showing zero ([Cancel and interrupt](../cards/the-vault.md#cancel-and-interrupt)). | A vault never loaded on this device, then airplane mode. | 1. Clear site data, load the vault once to establish identity, then airplane mode.<br>2. Reload the vault. | Tiles draw with initials where art is missing, the shelves and roster still render, and the header counters are blank rather than zero. | — |
| VLT-50 | P2 | phone, member | A member whose token has expired loses the server half and the roster still renders, with no retry storm ([Cancel and interrupt](../cards/the-vault.md#cancel-and-interrupt)). | A member with trophies and a dust balance, on a desktop with the network panel open. | 1. Clear `wwbh:member-token`.<br>2. Reload the vault.<br>3. Watch the network panel for a minute. | Trophies, the dust chip and the collected count stop resolving; the roster is still drawn. The collection query gives up after one refusal rather than retrying in a loop. | — |
| VLT-51 | P1 | phone, member | The secret sheet swipes through every secret on the page in shelf order, including secrets on shelves that are currently rolled up (suspected bug) ([Open questions and verification](../cards/the-vault.md#open-questions-and-verification)). | A vault holding secrets on at least two shelves, with one of those shelves rolled up. | 1. Roll one secret shelf up.<br>2. Open a card from an open shelf.<br>3. Swipe forward past the end of that shelf's cards. | Record what happens. Triage entry B-25 says the swipe lands on a card from the closed shelf; the sheet's own promise is that it swipes what is on screen. | — |

Not checkable by hand:

- The order a shelf returns to after it has been emptied and comes back — a set whose last card was traded away and then traded back ([Open questions and verification](../cards/the-vault.md#open-questions-and-verification)). It needs a trade in each direction and a stored order the tester cannot inspect meaningfully.
- Whether the grid visibly settles exactly once when the collection reconciles, and how long that takes on a garden network ([Open questions and verification](../cards/the-vault.md#open-questions-and-verification)) — a measurement rather than a pass or fail.
- Whether a trophy earned before the trophy table existed prints no date ([Edge cases](../cards/the-vault.md#edge-cases)); it needs a backfilled row this build cannot create.
- Whether the assumption that no control on this screen writes to the server still holds ([Open questions and verification](../cards/the-vault.md#open-questions-and-verification)) — a source claim, though VLT-18's offline pass is the closest observable.
